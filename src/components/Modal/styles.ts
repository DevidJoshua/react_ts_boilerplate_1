import { css } from 'emotion';

export const styBoxmodal = css`
  background-color: #ffffff;
  border-radius: 4px;
  z-index: 10;
  position: absolute;
  width: calc(100% - 32px);
  padding: 16px;
  top: 20%;
  left: 50%;
  transform: translate(-50%, -20%);
  margin: 0 auto;
  max-width: 500px;
`;
